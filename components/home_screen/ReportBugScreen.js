import { StyleSheet, View } from 'react-native';
import { Text } from 'react-native-paper';

export const ReportBugScreen = props => {
  return (
    <View>
      <Text variant={'displayLarge'}>Report Bug</Text>
    </View>
  )
}

const styles = StyleSheet.create({

});